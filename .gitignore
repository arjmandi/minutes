# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Env / secrets
.env
*.local

# Planning artifacts
.minutesplan_extracted/
minutesplan.zip

# Local audio samples (may contain PII — never commit)
*.m4a
*.wav

# Capture driver runtime
node_modules/
capture/driver/.profiles/
capture/driver/package-lock.json
